{% extends 'table.html.twig' %}

{% block body %}

    <form method="POST" id="my_form">
    <table  class="table" >
        <thead>
        <tr>
            <th>N°inscription</th>
            <th>nom</th>
            <th>prenom</th>
            <th>note</th>
        </tr>
        </thead>
        {% for note in notes %}
            <tr>
                <td>{{ note.etudiant.numInscription }}</td>
                <td>{{ note.etudiant.nom }}</td>
                <td>{{ note.etudiant.prenom }}</td>
                <td><input type="number"
                           min="0"
                           max="20.00"
                           step="0.25"
                           id="my_form"
                           name="{{note.etudiant.id}}"
                           value={{note.tmpNote}} ></td>
            </tr>
        {% endfor %}

    </table>
        <button class="btn btn-primary form-btn" type="submit" class="btn btn-default">Save</button>
    </form>
{% endblock %}